{
  "name": "@omni-shield/relayer",
  "version": "1.0.0",
  "description": "Omni-Shield XCM Relayer — Off-chain service for cross-chain dispatch confirmation and yield oracle",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "echo \"Backend tests: see contracts/ for smart contract tests. Relayer service has no unit tests yet.\" && exit 0"
  },
  "keywords": ["polkadot", "xcm", "relayer", "defi"],
  "author": "Omni-Shield Team",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.3.1",
    "ethers": "^6.16.0",
    "winston": "^3.19.0"
  }
}
